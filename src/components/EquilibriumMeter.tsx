import { usePlanner } from "../state/PlannerContext";
export function EquilibriumMeter({ compact = false }: { compact?: boolean }) { const { report } = usePlanner(); const pct = Math.max(4, Math.min(100, 46 + report.netTension)); return <section className={`equilibrium glass-panel ${report.status} ${compact ? "compact" : ""}`}><div className="eq-head"><span /><strong>{report.status === "balanced" ? "Balanced" : report.status === "thinIce" ? "Thin ice" : "Red zone"}</strong></div><div className="eq-track"><i style={{ width: `${pct}%` }} /><b /><em /></div>{!compact && <div className="eq-stats"><span>{report.tension}</span><span>{report.relief}</span><span>{report.netTension}</span></div>}{report.sealDisabledReason && <p>{report.sealDisabledReason}</p>}</section>; }
